defaults:
  - ema
  - model_checkpoint
  # - early_stopping
  # - learning_rate_monitor
  - _self_

model_checkpoint:
  dirpath: ${paths.output_dir}/checkpoints
  filename: "ldm-epoch@{epoch}-step@{step}-val_qmof150_all_checks@{val_qmof150/all_checks:.4f}"
  monitor: "val_qmof150/all_checks"
  mode: "max"
  save_last: True

# early_stopping:
#   monitor: "val/valid_rate"
#   patience: 100
#   mode: "max"
